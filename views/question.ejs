<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Pregunta <%= number %> - Perfil Humano Digital</title>
  <style>
    * {
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body {
      margin: 0;
      background: linear-gradient(180deg, #eef4fb 0%, #f8fbff 100%);
      color: #0f172a;
      padding: 24px;
    }

    .topbar {
      max-width: 1200px;
      margin: 0 auto 20px auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
      flex-wrap: wrap;
    }

    .topbar-left,
    .topbar-right {
      display: flex;
      gap: 12px;
      align-items: center;
      flex-wrap: wrap;
    }

    .topbar a {
      text-decoration: none;
      color: #2563eb;
      font-weight: bold;
      background: rgba(255, 255, 255, 0.78);
      padding: 10px 14px;
      border-radius: 999px;
      border: 1px solid #dbe3ea;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      background: #ffffff;
      border-radius: 24px;
      padding: 32px;
      box-shadow: 0 14px 34px rgba(15, 23, 42, 0.08);
      border: 1px solid #e5e7eb;
    }

    .header {
      margin-bottom: 24px;
    }

    .header h1 {
      margin: 0 0 8px 0;
      font-size: 2rem;
    }

    .subtitle {
      margin: 0;
      color: #64748b;
      line-height: 1.5;
    }

    .progress-wrap {
      margin-bottom: 28px;
      padding: 18px 20px;
      background: #f8fafc;
      border: 1px solid #e5e7eb;
      border-radius: 18px;
    }

    .progress-top {
      display: flex;
      justify-content: space-between;
      gap: 12px;
      flex-wrap: wrap;
      margin-bottom: 10px;
      font-size: 0.95rem;
      color: #475569;
      font-weight: bold;
    }

    .progress-bar {
      width: 100%;
      height: 14px;
      background: #e5e7eb;
      border-radius: 999px;
      overflow: hidden;
    }

    .progress-fill {
      height: 100%;
      background: linear-gradient(90deg, #2563eb, #22c55e);
      border-radius: 999px;
    }

    .question-layout {
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      gap: 24px;
      align-items: start;
      margin-bottom: 24px;
    }

    .left-panel,
    .right-panel {
      background: #f8fbff;
      border: 1px solid #dbe7f3;
      border-radius: 22px;
      padding: 24px;
    }

    .question-text {
      font-size: 1.22rem;
      font-weight: bold;
      line-height: 1.6;
      margin-bottom: 20px;
      text-align: justify;
    }

    .question-image-wrap {
      display: flex;
      justify-content: center;
    }

    .question-image-card {
      width: 100%;
      background: #ffffff;
      border: 1px solid #dbe3ea;
      border-radius: 20px;
      padding: 16px;
    }

    .question-image-card img {
      width: 100%;
      max-height: 500px;
      object-fit: contain;
      display: block;
      margin: 0 auto;
      border-radius: 14px;
      background: #ffffff;
    }

    .question-image-note {
      margin-top: 10px;
      text-align: center;
      color: #64748b;
      font-size: 0.92rem;
    }

    .no-image-box {
      min-height: 220px;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      border: 1px dashed #cbd5e1;
      border-radius: 18px;
      color: #64748b;
      background: #fff;
      padding: 20px;
      line-height: 1.5;
    }

    .answer-title {
      font-size: 1.1rem;
      font-weight: bold;
      margin-bottom: 16px;
    }

    label {
      display: block;
      margin-bottom: 10px;
      font-weight: bold;
      color: #0f172a;
    }

    textarea,
    select,
    input[type="text"] {
      width: 100%;
      padding: 14px 16px;
      border-radius: 14px;
      border: 1px solid #cbd5e1;
      font-size: 1rem;
      margin-bottom: 20px;
      background: #fff;
      outline: none;
    }

    textarea:focus,
    select:focus,
    input[type="text"]:focus {
      border-color: #2563eb;
      box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.08);
    }

    textarea {
      min-height: 150px;
      resize: vertical;
      line-height: 1.5;
    }

    .radio-group {
      display: grid;
      gap: 12px;
      margin-bottom: 24px;
    }

    .radio-item {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 14px 16px;
      border: 1px solid #dbe3ea;
      border-radius: 14px;
      background: #ffffff;
      transition: 0.2s ease;
    }

    .radio-item:hover {
      border-color: #93c5fd;
      background: #f8fbff;
    }

    .radio-item label {
      margin: 0;
      font-weight: normal;
      cursor: pointer;
      width: 100%;
      line-height: 1.4;
    }

    .actions {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
      align-items: center;
      margin-top: 8px;
    }

    button,
    .secondary-link {
      padding: 14px 22px;
      border: none;
      border-radius: 999px;
      font-size: 1rem;
      font-weight: bold;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
    }

    button {
      background: #2563eb;
      color: white;
      box-shadow: 0 8px 18px rgba(37, 99, 235, 0.18);
    }

    button:hover {
      background: #1d4ed8;
    }

    .secondary-link {
      background: #e5e7eb;
      color: #111827;
    }

    .helper {
      margin-top: 16px;
      color: #64748b;
      font-size: 0.95rem;
      line-height: 1.5;
    }

    @media (max-width: 960px) {
      .question-layout {
        grid-template-columns: 1fr;
      }

      .question-text {
        text-align: left;
      }
    }

    @media (max-width: 768px) {
      body {
        padding: 16px;
      }

      .container {
        padding: 22px;
      }

      .header h1 {
        font-size: 1.7rem;
      }

      .question-text {
        font-size: 1.08rem;
      }

      .left-panel,
      .right-panel {
        padding: 18px;
      }

      .question-image-card {
        padding: 10px;
      }

      .question-image-card img {
        max-height: 320px;
      }

      .actions {
        flex-direction: column;
        align-items: stretch;
      }

      button,
      .secondary-link {
        width: 100%;
        text-align: center;
      }
    }
  </style>
</head>
<body>
  <div class="topbar">
    <div class="topbar-left">
      <strong>Perfil Humano Digital · Pregunta <%= number %> / <%= totalQuestions %></strong>
    </div>

    <div class="topbar-right">
      <a href="<%= mode === 'edit' ? '/questionnaire/edit-answers' : '/questionnaire/instructions' %>">
        <%= mode === 'edit' ? 'Volver a editar respuestas' : 'Volver al resumen' %>
      </a>
      <a href="/logout">Cerrar sesión</a>
    </div>
  </div>

  <div class="container">
    <div class="header">
      <h1>Pregunta <%= number %></h1>
      <p class="subtitle">Observa la imagen de referencia y responde con la opción que mejor te describa.</p>
    </div>

    <div class="progress-wrap">
      <div class="progress-top">
        <span>Avance general</span>
        <span><%= progressPercent.toFixed(1) %>%</span>
      </div>
      <div class="progress-bar">
        <div class="progress-fill" style="width: <%= progressPercent %>%;"></div>
      </div>
    </div>

    <form action="/questionnaire/question/<%= number %>" method="POST">
      <input type="hidden" name="mode" value="<%= mode %>" />

      <div class="question-layout">
        <div class="left-panel">
          <div class="question-text"><%= question.text %></div>

          <% if (question.image) { %>
            <div class="question-image-wrap">
              <div class="question-image-card">
                <img src="<%= question.image %>" alt="Imagen de apoyo de la pregunta" />
                <div class="question-image-note">Imagen de referencia para responder esta pregunta</div>
              </div>
            </div>
          <% } else { %>
            <div class="no-image-box">
              Esta pregunta no contiene imagen de apoyo.<br />
              Puedes responder directamente en el panel derecho.
            </div>
          <% } %>
        </div>

        <div class="right-panel">
          <div class="answer-title">Tu respuesta</div>

          <% if (question.type === 'select') { %>
            <label for="answer">Selecciona la opción que mejor te describe:</label>
            <select id="answer" name="answer" required>
              <option value="">Selecciona una opción...</option>
              <% question.options.forEach(function(opt) { %>
                <option value="<%= opt %>" <%= existingAnswer === opt ? 'selected' : '' %>><%= opt %></option>
              <% }); %>
            </select>

          <% } else if (question.type === 'radio') { %>
            <label>Selecciona la opción que mejor te describe:</label>
            <div class="radio-group">
              <% question.options.forEach(function(opt, index) { %>
                <div class="radio-item">
                  <input
                    type="radio"
                    id="opt_<%= index %>"
                    name="answer"
                    value="<%= opt %>"
                    <%= existingAnswer === opt ? 'checked' : '' %>
                    required
                  />
                  <label for="opt_<%= index %>"><%= opt %></label>
                </div>
              <% }); %>
            </div>

          <% } else { %>
            <label for="answer">Escribe tu respuesta:</label>
            <textarea id="answer" name="answer" required><%= existingAnswer || '' %></textarea>
          <% } %>

          <div class="actions">
            <button type="submit">
              <%= number === totalQuestions && mode === 'normal' ? 'Guardar y finalizar' : 'Guardar y continuar' %>
            </button>

            <a class="secondary-link" href="/questionnaire/edit-answers">Ir al resumen</a>
          </div>

          <div class="helper">
            <% if (mode === 'normal' && number < totalQuestions) { %>
              Tu respuesta se guardará y avanzarás automáticamente a la siguiente pregunta.
            <% } else if (mode === 'edit') { %>
              Tu cambio se guardará y volverás al panel de edición de respuestas.
            <% } else { %>
              Tu respuesta se guardará correctamente.
            <% } %>
          </div>
        </div>
      </div>
    </form>
  </div>
</body>
</html>