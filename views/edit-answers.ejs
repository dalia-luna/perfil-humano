<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Editar respuestas - Perfil Humano Digital</title>
  <style>
    * {
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body {
      margin: 0;
      background: #eaf0f6;
      padding: 24px;
      color: #111827;
    }

    .topbar {
      max-width: 1100px;
      margin: 0 auto 20px auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
      flex-wrap: wrap;
    }

    .topbar a {
      text-decoration: none;
      color: #2563eb;
      font-weight: bold;
      margin-left: 12px;
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      background: #fff;
      border-radius: 20px;
      padding: 32px;
      box-shadow: 0 10px 28px rgba(0, 0, 0, 0.08);
    }

    h1 {
      margin-bottom: 10px;
    }

    .subtitle {
      margin-bottom: 24px;
      color: #6b7280;
    }

    .grid {
      display: grid;
      gap: 16px;
    }

    .card {
      border: 1px solid #e5e7eb;
      border-radius: 16px;
      padding: 18px;
      background: #f8fafc;
    }

    .card-top {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
      flex-wrap: wrap;
      margin-bottom: 8px;
    }

    .pill {
      padding: 6px 10px;
      border-radius: 999px;
      font-size: 0.85rem;
      font-weight: bold;
    }

    .pill-ok {
      background: #dcfce7;
      color: #166534;
    }

    .pill-empty {
      background: #fee2e2;
      color: #991b1b;
    }

    .question-text {
      margin: 10px 0 14px 0;
      line-height: 1.5;
    }

    .answer-box {
      margin: 10px 0 14px 0;
      padding: 12px;
      border-radius: 12px;
      background: #fff;
      border: 1px solid #dbe3ea;
      color: #374151;
    }

    .actions a {
      display: inline-block;
      padding: 10px 16px;
      border-radius: 999px;
      text-decoration: none;
      font-weight: bold;
      background: #2563eb;
      color: white;
    }
  </style>
</head>
<body>
  <div class="topbar">
    <div><strong>Perfil Humano Digital · Editar respuestas</strong></div>
    <div>
      <a href="/questionnaire/instructions">Volver al resumen</a>
      <a href="/logout">Cerrar sesión</a>
    </div>
  </div>

  <div class="container">
    <h1>Selecciona la pregunta que deseas modificar</h1>
    <p class="subtitle">
      Has completado las <%= totalQuestions %> preguntas. Aquí puedes revisar o cambiar cualquier respuesta.
    </p>

    <div class="grid">
      <% questions.forEach(function(q, index) { 
           const num = index + 1;
           const answered = answersByQuestion[num];
      %>
        <div class="card">
          <div class="card-top">
            <strong>Pregunta <%= num %></strong>
            <% if (answered) { %>
              <span class="pill pill-ok">Contestada</span>
            <% } else { %>
              <span class="pill pill-empty">Sin respuesta</span>
            <% } %>
          </div>

          <div class="question-text"><%= q.text %></div>

          <div class="answer-box">
            <strong>Respuesta actual:</strong>
            <div><%= answered || 'Sin respuesta registrada' %></div>
          </div>

          <div class="actions">
            <a href="/questionnaire/question/<%= num %>?mode=edit">Editar</a>
          </div>
        </div>
      <% }); %>
    </div>
  </div>
</body>
</html>